apiVersion: v1
kind: Service
metadata:
  name: deliveryrisk-drift
  namespace: deliveryrisk
  labels:
    app: deliveryrisk-drift
spec:
  selector:
    app: deliveryrisk-drift
  ports:
    - name: http
      port: 8010
      targetPort: 8010

